{
  "name": "heyagent",
  "version": "2.0.0",
  "description": "HeyAgent.dev - Bidirectional Telegram bridge for Claude Code and Codex",
  "main": "src/bridge.js",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "bin": {
    "hey": "./bin/hey.js"
  },
  "scripts": {
    "start": "./bin/hey.js",
    "dev:watch:claude": "node --watch --watch-path=src --watch-path=bin --watch-preserve-output ./bin/hey.js claude",
    "dev:watch:codex": "node --watch --watch-path=src --watch-path=bin --watch-preserve-output ./bin/hey.js codex",
    "dev:watch:status": "node --watch --watch-path=src --watch-path=bin --watch-preserve-output ./bin/hey.js status",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npm run lint && npm run format:check",
    "pack": "npm pack",
    "pack:install": "npm pack && npm install -g heyagent-*.tgz",
    "pack:test": "npm pack && mkdir -p .tmp && cd .tmp && npm init -y && npm install ../heyagent-*.tgz && ./node_modules/.bin/hey --version",
    "version:patch": "npm version patch",
    "version:minor": "npm version minor",
    "version:major": "npm version major",
    "release:patch": "npm run check && npm version patch && npm publish && git push --follow-tags",
    "release:minor": "npm run check && npm version minor && npm publish && git push --follow-tags",
    "release:major": "npm run check && npm version major && npm publish && git push --follow-tags",
    "release:beta": "npm run check && npm version prerelease --preid=beta && npm publish --tag beta && git push --follow-tags"
  },
  "keywords": [
    "claude",
    "codex",
    "telegram",
    "cli",
    "terminal",
    "heyagent"
  ],
  "author": "HeyAgent Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/gergomiklos/heyagent.git"
  },
  "homepage": "https://heyagent.dev",
  "bugs": {
    "url": "https://github.com/gergomiklos/heyagent/issues"
  },
  "files": [
    "bin/",
    "src/",
    "README.md",
    "LICENSE"
  ],
  "dependencies": {
    "@inquirer/prompts": "^8.3.0",
    "cloudflared": "^0.7.1",
    "cross-spawn": "^7.0.6",
    "node-telegram-bot-api": "^0.66.0",
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.0.0"
  }
}
